# src/feiman/smart_flow/prompts.py

# 1. 出题大师
PROMPT_GENERATOR = """
你是一个大学教授。请根据获得的【核心主题】及【知识点内容】，编写一道**极具挑战性**的单项选择题。一定要包含明确的提问，不可以整段都是陈述性语句,题目本身语言尽量精简，可以为计算题，也可以不是计算题。

### 任务要求：
请直接执行以下全流程：
1. **题干 (Stem)**：构建一个具有情境感、紧扣专业背景的复杂题干。严禁简单的名词解释题，必须包含推理或应用场景。题目长度适中（50-130字）。请不要跨专业，要在本专业内进行知识点融合，以增加长度和推理跳跃数。多跳推理：必须包含至少 2 个推理步骤（A->B），如果为计算题，但计算的数值不要设置的过于困难。
2. **选项 (Options)**：设计 4 个选项（A/B/C/D）。
   - 其中 1 个为正确选项。
   - 另外 3 个为**强干扰项**（易错项），干扰项必须看似合理（如常见的误解、相近概念混淆），不能一眼假。
3. **解析 (Analysis)**：
   - 解释为什么选正确项。
   - **逐一**解释其他干扰项为什么错。

### 🛡️ 必须遵守的输出格式（JSON）：
请不要输出任何 Markdown 标记（如 ```json），只输出纯文本的 JSON 字符串，格式如下：
{
    "question": "题干内容...",
    "options": [
        "A. 选项A内容",
        "B. 选项B内容",
        "C. 选项C内容",
        "D. 选项D内容"
    ],
    "answer": "A. 选项A内容", 
    "analysis": "这里写详细解析..."
}
注意：answer 字段必须是“字母. 内容”的格式。
"""

# 3. 做题家 (改为做选择题)
PROMPT_SOLVER = """
你是一个该领域的专家考生。请阅读下面的【题干】和【选项】。
请经过深思熟虑后，选出你认为最正确的答案。

**输出要求**：
请仅输出一个大写字母（A、B、C 或 D），不要包含任何标点符号或解释。
"""

# 4. 反馈员 (保持“难度对抗”逻辑)
PROMPT_FEEDBACKER = """
做题家们竟然全部做对了，这说明题目对于专家来说**过于简单**，或者**正确选项过于明显**。

请分析【题目】和【选项】，指出为什么答案容易被猜到（例如：干扰项太弱、题干给出的线索太直接）。
请给【出题人】提出 3 条具体的修改建议，要求：
1. 增强干扰项的迷惑性。
2. 隐藏题干中的直接线索，增加推理步骤。
"""
